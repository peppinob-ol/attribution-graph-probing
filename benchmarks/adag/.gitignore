# local venvs and secrets — never commit
.venvs/
*.env
.env
methods/adag/.env
__pycache__/
*.pyc
# runs artifacts are tracked by DVC (the .dvc pointers are committed, not the blobs)
runs/*
!runs/.gitkeep
!runs/*.dvc
