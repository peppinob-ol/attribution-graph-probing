This directory holds temporary copies of remote files that are patched locally
and then uploaded back to the remote GPU node.

Files here are not imported by the local project; they are only used as staging
for remote hotfixes.


